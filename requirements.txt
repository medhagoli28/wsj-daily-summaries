# The fetch layer (Mode A) and de-duplication (dedup.py) are stdlib-only — free,
# no dependencies. The only dependency is the Anthropic SDK, needed solely for the
# optional, opt-in deep-research mode (`--research`, which makes paid API calls).
anthropic>=0.40
